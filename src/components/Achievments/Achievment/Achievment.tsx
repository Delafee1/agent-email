import * as React from 'react';

import s from './Achievment.module.scss';

type Props = {
  title: string;
  description: string;
  image: string;
  percentage: number;
};

const Achievment: React.FC<Props> = ({}) => {
  return <div>Achievment</div>;
};

export default Achievment;
